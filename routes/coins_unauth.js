// This route will be for accessing the collection in database that stores a list of coins saved by unauthorized users

const express = require('express');
const router = express.Router();
const config = require('../config/database');
const coin = require('../models/coin');

// get list of all the coins from collection

// add coin to collection

// delete coin from collection


module.exports = router;